Paxos:
- finish tests
- Implement NACKs
- Move agents over to their own protocols
- split Accept into AcceptRequest and AcceptNotify
- Add acceptor info to acceptnotify
- Move network logic into factory class
- Develop client separation logic

DBP:
short:
- hook into paxos
- fix up comments
- do we want assert_tx to insert no-op txs?
- hook ops into dbp.core
- serialize/deserialize ops
long:
- simple SQL parsing
- transactions
- extend command line client
